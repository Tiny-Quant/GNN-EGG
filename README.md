# GNN-EGG

Graph Neural Network Explanations via Graph Generation

The MUTAG results can be reproduced by cloning the repo and running the chunk below in the terminal. 

```
cd .devcontainer
docker compose build 
docker compose up 
```

